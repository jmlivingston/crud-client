const BASE_URL_BROKEN = 'http://localhost:4000/todos-broken-path/'
const BASE_URL = 'http://localhost:4000/todos/'

const STORAGE_KEYS = Object.freeze({
  SESSION: 'SESSION',
})

export { BASE_URL, BASE_URL_BROKEN, STORAGE_KEYS }
